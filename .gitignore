__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
storage/

# Session transcripts pasted in for context, not source.
2026-*-*-*-*.txt
.playwright-mcp/

# Legacy production dumps. Real student names, addresses and password hashes --
# they are read at cutover and kept off the repository for good.
legacy-db.dump
legacy-db.sql.gz
legacy-*.dump
legacy-*.sql.gz
legacy-save/

# Screenshots. A backend has no images of its own, so anything that lands here
# is somebody's console capture -- and one has been committed by accident
# already.
*.png
*.jpg
*.jpeg
